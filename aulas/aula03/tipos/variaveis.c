#include <stdio.h> 

int main() {

  int numero;

  numero = 10;

  numero = 100; //ele pode ser mudado por ser uma variavel

  const float PI = 3,14  //constante nao pode ser mudada por ser uma constante

    int sexo = 1;
    const int sexo_masculino = 1;

  sexo = sexo_masculino; //valor fixo = constante (const)

   #define PI 3,14f; 
  //define uma constante
  //constante nao pode ser minuscula
    
  return 0;
}